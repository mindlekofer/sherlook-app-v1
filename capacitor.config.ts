import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'sherlook.app.v1',
  appName: 'sherlook-app-v1',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
